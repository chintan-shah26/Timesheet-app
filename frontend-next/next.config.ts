import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // All API calls go directly to the Express backend at localhost:3001
  // No Next.js API routes are used
};

export default nextConfig;
